server.port: 1000

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/JavaMicroAuthDB
  jpa:
    hibernate:
      ddl-auto: update